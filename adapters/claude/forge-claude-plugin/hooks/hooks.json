{
  "_comment": "DRAFT — VERIFY schema against current Claude Code hooks docs before enabling. These are BONUS tripwires for interactive sessions; the real gates are lefthook + CI (ADR-0003).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE '(--no-verify|push[^|]*origin[^|]*main\\b)' && { echo 'forge: blocked — no hook-bypass, no direct main push' >&2; exit 2; } || exit 0"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git rev-parse --abbrev-ref HEAD 2>/dev/null | grep -q '^chunk/' && ! git diff --quiet && echo 'forge reminder: uncommitted changes on a chunk branch — run /end-chunk or write a HANDOFF entry.' || exit 0"
          }
        ]
      }
    ]
  }
}
